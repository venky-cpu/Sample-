I gave you that prompt in my last message — it's ready to copy. Let me make it easier to use by giving you a clean version with nothing extra around it, so you can just select and paste.

---

You are a senior Java backend engineer. Build a document search REST API. Follow every rule exactly. Produce compile-ready code, no syntax errors, clean imports, Javadoc on public classes and methods.

**Tech stack (mandatory — do not change versions)**
- Java 21 (use records, switch expressions)
- Spring Boot 3.5.14
- Spring Data Elasticsearch (version managed by Spring Boot 3.5.14). Use the new client `co.elastic.clients.elasticsearch.ElasticsearchClient`. Do NOT use the deprecated `RestHighLevelClient`.
- Jackson, Jakarta Validation

**Endpoint:** `GET /api/v1/documents/search`

**Query parameters (all optional):**
1. `identifierContains` (String) — substring filter on the identifier field.
2. `exists` (String) — a field name; return only docs where that field is present.
3. `pageSize` (Integer, default 20, must be 1–1000).
4. `order` (String `ASC`/`DESC`, case-insensitive, default `ASC`).
5. `nextCursor` (String) — opaque pagination token; blank/absent means first page.

**Rules:**

1. **No offset pagination.** Millions of documents. Do NOT use `from`/`size`. Use Elasticsearch `search_after` cursor pagination.

2. **Stable unique sort.** Sort on `metadata._id.$oid` (unique Mongo ObjectId, must be a `keyword` field). Apply the `order` (ASC/DESC) to it. Put the field in a single `SORT_FIELD` constant.

3. **Opaque cursor.** `nextCursor` is a URL-safe Base64 token encoding the JSON list of the last hit's `sort` values. Build a `CursorCodec` with `encode(List<FieldValue>)` and `decode(String)`. If decoding fails, throw an exception that maps to HTTP 400.

4. **Optional independent filters, combined in a `bool` query, filter context (no scoring):**
   - `identifierContains` present → case-insensitive wildcard `*value*` on `SORT_FIELD`.
   - `exists` present → ES `exists` filter on that field name.
   - Param absent/blank → skip that clause.
   - No filters at all → `match_all`.

5. **Configurable index name** from `app.search.index-name`, default `elastic_index`. Single index only.

6. **Next cursor logic:** after the query, if returned hits count equals `pageSize` (full page) → build `nextCursor` from the last hit's sort values; if fewer (last page) → `nextCursor` = `null`.

**Response JSON:**
- `queryTimeMs` (long) — measured with `System.nanoTime()` around the query.
- `total` (long) — enable `track_total_hits`.
- `docs` — list of `{ id (ES _id), score (Double, nullable), metadata (Map<String,Object> = _source) }`.
- `nextCursor` (String, nullable).
- `actionDate` — current server time, ISO-8601 with offset, pattern `yyyy-MM-dd'T'HH:mm:ss.SSSXXX` (e.g. `2026-06-20T15:24:32.864+05:30`).

**Error handling (`@RestControllerAdvice`):**
- Bad `pageSize` (out of range), invalid `order`, type mismatch, missing param, malformed `nextCursor` → HTTP 400 with a meaningful validation message.
- Elasticsearch unavailable / query fails → HTTP 500 with a friendly message; log the real exception server-side.
- Consistent error DTO: `status`, `error`, `message`, `details` (list), `timestamp`. Never leak stack traces.

**Classes to create (package `com.example.search`):**
1. `dto/SearchRequest` — record of the 5 params; defaults (pageSize=20, order=ASC) in compact constructor; `hasCursor()` helper.
2. `dto/SortOrder` — enum `ASC`/`DESC` with a method mapping to the ES client `SortOrder`.
3. `dto/DocumentHit` — record: `id`, `score`, `metadata`.
4. `dto/SearchResponse` — record: `queryTimeMs`, `total`, `docs`, `nextCursor`, `actionDate` (with the `@JsonFormat` pattern above).
5. `service/CursorCodec` — encode/decode cursor; handle all `FieldValue` kinds (String, Long, Double, Boolean, Null).
6. `service/DocumentSearchService` — builds the bool query (Rule 4), builds the ES `SearchRequest` with `size`, sort on `SORT_FIELD`, `track_total_hits`, and `search_after` (when a cursor is present), executes `client.search(...)`, maps hits to `DocumentHit`, computes `nextCursor` (Rule 6), returns `SearchResponse`; wrap ES exceptions in `SearchBackendException`.
7. `controller/DocumentSearchController` — `@RestController` for the GET endpoint; `@Min(1)/@Max(1000)` on `pageSize`; build `SearchRequest`; return `ResponseEntity.ok(...)`.
8. `config/WebConfig` — case-insensitive `String -> SortOrder` converter; unknown value throws → 400.
9. `config/SearchConfig` — `@Bean` for `CursorCodec` (inject Spring `ObjectMapper`).
10. `exception/` — `ApiError` (record), `InvalidCursorException` (→400), `SearchBackendException` (→500), `GlobalExceptionHandler` (`@RestControllerAdvice`) mapping `MethodArgumentNotValidException`, `ConstraintViolationException`, `MethodArgumentTypeMismatchException`, `MissingServletRequestParameterException`, `IllegalArgumentException`, `InvalidCursorException` → 400; `SearchBackendException` + generic `Exception` → 500.

**Also provide:**
- `application.yml` with `spring.elasticsearch.uris`, connection/socket timeouts, `app.search.index-name`.
- `pom.xml` dependencies: `spring-boot-starter-web`, `spring-boot-starter-validation`, `spring-boot-starter-data-elasticsearch`; parent `spring-boot-starter-parent:3.5.14`; `java.version=21`.

**Note in a comment:** `metadata._id.$oid` must be mapped as `keyword` for `search_after`, sorting, and wildcard filtering to work. If the real path differs, only `SORT_FIELD` needs changing.

Deliver all files fully implemented, compile-ready, with imports and Javadoc.

---

That's the whole thing. Select from "You are a senior Java backend engineer" down to "with imports and Javadoc" and paste it into your company's AI tool.

If their tool has a length limit and cuts it off, tell me and I'll compress it to a shorter version.
